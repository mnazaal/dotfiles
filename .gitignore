.agent-shell
.netrwhist
.viminfo
.git-credentials
.config/git/credentials/
.envrc
.direnv/
*.pem
*.key
*.p12
*.pfx
.ssh/known_hosts
id_rsa
id_rsa.pub
id_ed25519
id_ed25519.pub
credentials.json
secrets.json
.config/gh/hosts.yml
.agents/skills/synced/

# onedrive: the client keeps refresh_token and items.sqlite3 in its config
# directory, which is this stow-managed one. Default-deny it so a token can
# never be committed, and track only the config file itself.
.config/onedrive/*
!.config/onedrive/config

# Never commit full Firefox profiles or runtime/secrets files.
.mozilla/firefox/*
!.mozilla/firefox/bpw3h4pa.BetterFox/
.mozilla/firefox/bpw3h4pa.BetterFox/*
!.mozilla/firefox/bpw3h4pa.BetterFox/user.js
!.mozilla/firefox/bpw3h4pa.BetterFox/chrome/
!.mozilla/firefox/bpw3h4pa.BetterFox/chrome/**
.config/mozilla/firefox/

# Local editor/system noise
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# Local env/secrets files
.env
.env.*
*.secret
*.secrets

# Common build/cache artifacts
node_modules/
coverage/
dist/
__pycache__/
.pytest_cache/

# Local agent/tool state
.kiro/

# Claude Code: track the curated config, ignore runtime/state and local overrides
.claude/*
!.claude/CLAUDE.md
!.claude/settings.json
!.claude/keybindings.json
!.claude/install-mcp.sh
!.claude/agents/
!.claude/hooks/
!.claude/skills

# Standing plan and personal notes — local working documents, deliberately untracked
PLAN.md
wip.org
notes/

# Assets
.local/share/*
# gpg-agent cache TTLs: the setting that keeps tty-less cron jobs able to
# decrypt. Nothing secret here; the keyring itself stays ignored.
!.local/share/gnupg/
.local/share/gnupg/*
!.local/share/gnupg/gpg-agent.conf

# Logs
*.log
*.bak-*
